---
title: JSON
description: JSON library, useful for sending data from Client's Package to WebUI environment
tags: [utility-class]
---

<HeaderDeclaration type="UtilityClass" name="JSON" is_static is_open_source />

:::tip

nanos world implements a lightweight JSON library from [https://github.com/rxi/json.lua](https://github.com/rxi/json.lua).

:::


## 🎒 Examples

```lua
local encoded_value = JSON.stringify({ 1, 2, 3, { x = 10 } })
-- Returns '[1,2,3,{"x":10}]'

local decoded_value = JSON.parse('[1,2,3,{"x":10}]')
-- Returns { 1, 2, 3, { x = 10 } }
```


:::caution

Note that custom classes (e.g. **Vehicle**, **Vector**, **Character**… etc) aren't stringified with this functions and stringifying them will cause unknown behavior.

:::


## 🗿 Static Functions

<StaticFunctionsDeclaration type="UtilityClass" name="JSON" />
