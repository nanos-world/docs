---
description: Render provides on-screen methods and events related
tags: [static-class]
---

# 🖼️ Render

Render provides on-screen methods and events related

import { Structs, BasicType, AuthorityType, Classes, ReferenceLink, Enums } from '@site/docs/components/_nanos.mdx';

:::tip

This is a Static Class named `Render`. You can access it’s methods directly with `.`. It is not possible to initialize or create new instances.

This is a **Client** only Class.

:::

:::caution

If you are looking for how to make UIs, we recommend taking a look at <Classes.WebUI /> for a more robust and optimal system.

:::

<ReferenceLink href="core-concepts/scripting/user-interface">User Interface</ReferenceLink>

## Usage


```lua title="Client/Index.lua"
local text = "Hello" -- Text to render on the Canvas.
local location = Vector2D(123, 321) -- Screen space position to render the text.
local font_type = 0 -- Roboto
local font_size = 32 -- Size of the font
local text_color = Color(1, 0, 0, 1) -- Color to render the text.
local kerning = 0 -- Horizontal spacing adjustment to modify the spacing between each letter.
local shadow_color = Color(1, 1, 1, 1) -- Color to render the shadow of the text.
local shadow_offset = Vector2D(1, 1) -- Pixel offset relative to the screen space position to render the shadow of the text.
local is_centered_x = false -- If true, then interpret the screen space position X coordinate as the center of the rendered text.
local is_centered_y = false -- If true, then interpret the screen space position Y coordinate as the center of the rendered text.
local is_outlined = false -- If true, then the text should be rendered with an outline.
local enable_shadow = true -- If true, then shadow will be enabled
local outline_color = Color(1, 1, 1, 1) -- Color to render the outline for the text.

Render.AddText(0, text, location, font_type, font_size, text_color, kerning, is_centered_x, is_centered_y, enable_shadow, shadow_offset, shadow_color, is_outlined, outline_color)
```


## Functions

:::info

The following functions have an `group_id` parameter, which you can group related Canvas Items with an ID to facilitate the cleanup of them, or can give them Unique IDs. These return an `item_id` value, which can be used for updating specific Canvas Items afterwards.

:::

| **Returns** | **Name** | **Description** |
| :--- | :--- | :--- |
| <BasicType.Number /> | [**`AddLine`**](render#addline) | Draws a Line \(doesn’t support opacity\) |
| <BasicType.Number /> | [**`AddBox`**](render#addbox) | Draws a Box |
| <BasicType.Number /> | [**`AddTile`**](render#addtile) | Draws a Tile \(filled\) |
| <BasicType.Number /> | [**`AddPolygon`**](render#addpolygon) | Draws a Polygon |
| <BasicType.Number /> | [**`AddText`**](render#addtext) | Draws a Text with optionals Shadow and Outline |
| <Structs.Vector2D /> | [**`Project`**](render#project) | Transforms a 3D world-space vector into 2D screen coordinates. |
| <BasicType.Table /> | [**`Deproject`**](render#deproject) | Transforms a 2D screen coordinates into 3D world-space location |
|  | [**`ClearItems`**](render#clearitems) | Remove all drawing of using that specific group\_id |
|  | [**`Subscribe`**](render#subscribe) | Subscribes for an Event |
|  | [**`Unsubscribe`**](render#unsubscribe) | Unsubscribes from all subscribed Events in this Class and in this Package, optionally passing the function to unsubscribe only that callback |
|  | [**`UpdateItemText`**](render#updateitemtext) | Updates a specific Canvas’s position giving it’s group\_id and item\_id |
| <Structs.Vector2D /> | [**`GetViewportSize`**](render#getviewportsize) | Returns the size of viewport \(how much screen space the game window occupies\) |

## Events

| **Name** | **Description** |
| :--- | :--- |
| [**`ViewportResized`**](render#viewportresized) | Called when the screen is resized |

## Functions Detailed

### `AddLine`

> Draws a Line \(doesn't support opacity\)
>
> Returns <BasicType.Number /> `item_id`

```lua
Render.AddLine(group_id, start_position, end_position, thickness, color)
```

| Type | Parameter | Description |
| :--- | :--- | :--- |
| <BasicType.Number /> | **`group_id`** | The group ID to associate this item |
| <Structs.Vector2D /> | **`start_position`** | Screen position |
| <Structs.Vector2D /> | **`end_position`** | Screen position |
| <BasicType.Number /> | **`thickness`** | Line thickness |
| <Structs.Color /> | **`color`** | Line color |

### `AddBox`

> Draws a Box
>
> Returns <BasicType.Number /> item\_id

```lua
Render.AddBox(group_id, start_position, end_position, thickness, color)
```

| Type | Parameter | Description |
| :--- | :--- | :--- |
| <BasicType.Number /> | **`group_id`** | The group ID to associate this item |
| <Structs.Vector2D /> | **`start_position`** | Screen position |
| <Structs.Vector2D /> | **`end_position`** | Screen position |
| <BasicType.Number /> | **`thickness`** | Box thickness |
| <Structs.Color /> | **`color`** | Box color |

### `AddTile`

> Draws a Tile \(filled\)
>
> Returns <BasicType.Number /> item\_id

```lua
Render.AddTile(group_id, position, size, color)
```

| Type | Parameter | Description |
| :--- | :--- | :--- |
| <BasicType.Number /> | **`group_id`** | The group ID to associate this item |
| <Structs.Vector2D /> | **`position`** | Screen position |
| <Structs.Vector2D /> | **`size`** | Tile size |
| <Structs.Color /> | **`color`** | Box color |

### `AddPolygon`

> Draws a Polygon
>
> Returns <BasicType.Number /> item\_id

```lua
Render.AddPolygon(group_id, screen_position, radius,  number_of_sides, color)
```

| Type | Parameter | Description |
| :--- | :--- | :--- |
| <BasicType.Number /> | **`group_id`** | The group ID to associate this item |
| <Structs.Vector2D /> | **`start_position`** | Screen position |
| <Structs.Vector2D /> | **`radius`** | Polygon radius |
| <BasicType.Number /> | **`number_of_sides`** | Polygon number of sides |
| <Structs.Color /> | **`color`** | Box color |

### `AddText`

> Draws a Text with optionals Shadow and Outline
>
> Returns <BasicType.Number /> item\_id

```lua
Render.AddText(group_id, text, position, font_type, font_size, text_color, kerning, is_centered_x, is_centered_y, enable_shadow, shadow_offset, shadow_color, enable_outline,  outline_color)
```

| Type | Parameter | Description |
| :--- | :--- | :--- |
| <BasicType.Number /> | **`group_id`** | The group ID to associate this item |
| <BasicType.String /> | **`text`** | Text |
| <Structs.Vector2D /> | **`position`** | Screen position |
| <Enums>FontType</Enums> | **`font_type`** |  |
| <BasicType.Number /> | **`font_size`** |  |
| <Structs.Color /> | **`text_color`** |  |
| <BasicType.Number /> | **`kerning`** |  |
| <BasicType.Boolean /> | **`is_centered_x`** |  |
| <BasicType.Boolean /> | **`is_centered_y`** |  |
| <BasicType.Boolean /> | **`enable_shadow`** |  |
| <Structs.Vector2D /> | **`shadow_offset`** |  |
| <Structs.Color /> | **`shadow_color`** |  |
| <BasicType.Boolean /> | **`enable_outline`** |  |
| <Structs.Color /> | **`shadow_outline_color`** |  |

### `Project`

> Transforms a 3D world-space vector into 2D screen coordinates
>
> Returns Vector2D

```lua
Render.Project(world_position)
```

| Type | Parameter | Description |
| :--- | :--- | :--- |
| <Structs.Vector /> | **`world_position`** | World 3D position |

### `Deproject`

> Transforms a 2D screen coordinates into 3D world-space location
>
> Returns table in the format `{"Position", "Direction"}`

```lua
Render.Deproject(screen_position)
```

| Type | Parameter | Description |
| :--- | :--- | :--- |
| <Structs.Vector2D /> | **`screen_position`** | Screen position |

### `ClearItems`

> Remove all drawing of using that specific group\_id

```lua
Render.ClearItems(group_id)
```

| Type | Parameter | Description |
| :--- | :--- | :--- |
| <BasicType.Number /> | **`group_id`** | The group ID to delete all items |

### `Subscribe`

> Subscribes for an Event

```lua
Render.Subscribe(event_name, callback)
```

| Type | Parameter | Description |
| :--- | :--- | :--- |
| <BasicType.String /> | **`event_name`** | The Event Name to Subscribe for an event |
| <BasicType.Function /> | **`callback`** | The callback function to execute |

### `Unsubscribe`

> Unsubscribes from all subscribed Events in this Class and in this Package, optionally passing the function to unsubscribe only that callback

```lua
Render.Unsubscribe(event_name, callback)
```

| Type | Parameter | Default Value | Description |
| :--- | :--- | :--- | :--- |
| <BasicType.String /> | **`event_name`** |  | The Event Name to Unsubscribe |
| <BasicType.Function /> | **`callback`** | `nil` | The callback function to unsubscribe |

### `UpdateItemText`

> Updates a specific Canvas Text giving it's group\_id and item\_id

```lua
Render.UpdateItemText(group_id, item_id, new_text)
```

| Type | Parameter | Description |
| :--- | :--- | :--- |
| <BasicType.Number /> | **`group_id`** | The group ID to associate this item |
| <BasicType.Number /> | **`item_id`** | The item of the Text |
| <BasicType.String /> | **`new_text`** | Text |

### `UpdateItemPosition`

> Updates a specific Canvas's position giving it's group_id and item_id

```lua
Render.UpdateItemPosition(group_id, item_id, new_position)
```

| Type | Parameter | Description |
| :--- | :--- | :--- |
| <BasicType.Number /> | **`group_id`** | The group ID to associate this item |
| <BasicType.Number /> | **`item_id`** | The item of the Text |
| <Structs.Vector2D /> | **`new_position`** | Screen position |

### `GetViewportSize`

> Gets the size of viewport (how much screen space the game window occupies)
>
> Returns Vector2D

```lua
Render.GetViewportSize()
```

## Events Detailed

### `ViewportResized`

> Called when the screen is resized

```lua
Render.Subscribe("ViewportResized", function(new_size)

end)
```

| Type | Parameter | Description |
| :--- | :--- | :--- |
| <Structs.Vector2D /> | **`new_size`** | New new screen size |

