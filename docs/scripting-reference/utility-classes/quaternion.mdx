---
description: >-
  Floating point Quaternion that can represent a rotation about an axis in 3-D
  space
tags: [utility-class]
---

# Quat

Floating point Quaternion that can represent a rotation about an axis in 3-D space

import { BasicType, Structs } from '@site/docs/components/_nanos.mdx';

:::tip

This structure is Open Sourced at [https://github.com/nanos-world/nanos-world-lua-lib](https://github.com/nanos-world/nanos-world-lua-lib). Feel free to push merge requests and suggest changes!

:::

:::info

This structure supports `*`, `+`, `-` and `tostring` operations.

:::

## Usage

```lua
local new_quaternion = Quat(0.5, 0.5, 1, -1)
local rotation = new_quaterion:Rotator()
```

## Properties

| **Type** | **Name** | **Description** |
| :--- | :--- | :--- |
| <BasicType.Number /> | **`X`** | The quaternion's X-component |
| <BasicType.Number /> | **`Y`** | The quaternion's Y-component |
| <BasicType.Number /> | **`Z`** | The quaternion's Z-component |
| <BasicType.Number /> | **`W`** | The quaternion's W-component |

## Functions

| **Returns** | **Name** | **Description** |
| :--- | :--- | :--- |
|  | **`Normalize()`** | In place normalize this Quaternion |
| <Structs.Rotator /> | **`Rotator()`** | Get the Rotator representation of this Quaternion. |

