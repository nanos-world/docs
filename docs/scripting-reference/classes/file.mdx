---
description: A File represents an entry to a system file.
sidebar_position: 0
tags: [class]
---

# 📁 File

A File represents an entry to a system file.

import { Structs, BasicType, AuthorityType } from '@site/docs/components/_nanos.mdx';

:::info

This is a **Server** side class. It is not possible to open files from outside the server folder. All path must be relative to the Server’s executable folder. All files are opened as binary file by default.

:::

## Example


```lua
local configuration_file = File("my_awesome_configuration.json")

local configuration_file_json = JSON.parse(configuration_file:Read(configuration_file:Size()))
```

## Constructor Parameters

| **Type** | **Name** | **Default** | Description |
| :--- | :--- | :--- | :--- |
| <BasicType.String /> | **`file_path`** |  | Path relative to server executable |
| <BasicType.Boolean /> | **`truncate`** | `false` | Whether or not to clear the file upon opening it |

## Static Functions

| **Returns** | **Name** | **Description** |
| :--- | :--- | :--- |
| <BasicType.Number /> | [**`Time`**](file#time) | Returns when a file was last modified in Unix time |
| <BasicType.Boolean /> | [**`CreateDirectory`**](file#createdirectory) | Creates a Directory (for every folder passed\ |
| <BasicType.Number /> | [**`Remove`**](file#remove) | Deletes a folder or file |
| <BasicType.Boolean /> | [**`Exists`**](file#exists) | Verifies if a entry exists in the file system |
| <BasicType.Boolean /> | [**`IsDirectory`**](file#isdirectory) | Checks if a path is a directory |
| <BasicType.Boolean /> | [**`IsRegularFile`**](file#isregularfile) | Checks if a path is a file |

### `Time`

> Returns when the file was last modified in Unix time
>
> Returns <BasicType.Number />

```lua
File.Time(path)
```

| Type | Parameter | Description |
| :--- | :--- | :--- |
| <BasicType.String /> | **`path`** | Path to file |

### `CreateDirectory`

> Creates a directory \(for every folder passed\)
>
> Returns <BasicType.Boolean /> if succeeded

```lua
File.CreateDirectory(path)
```

| Type | Parameter | Description |
| :--- | :--- | :--- |
| <BasicType.String /> | **`path`** | Path to folder |

### `Remove`

> Deletes a folder or a file
>
> Returns <BasicType.Number />

```lua
File.Remove(path)
```

| Type | Parameter | Description |
| :--- | :--- | :--- |
| <BasicType.String /> | **`path`** | Path to file or folder |

### `Exists`

> Gets if a file or folder exists
>
> Returns <BasicType.Boolean />

```lua
File.Exists(path)
```

| Type | Parameter | Description |
| :--- | :--- | :--- |
| <BasicType.String /> | **`path`** | Path to file or folder |

### `IsDirectory`

> Gets if a path is a directory
>
> Returns <BasicType.Boolean />

```lua
File.IsDirectory(path)
```

| Type | Parameter | Description |
| :--- | :--- | :--- |
| <BasicType.String /> | **`path`** | Path to folder |

### `IsRegularFile`

> Gets if a path is a file
>
> Returns <BasicType.Boolean />

```lua
File.IsRegularFile(path)
```

| Type | Parameter | Description |
| :--- | :--- | :--- |
| <BasicType.String /> | **`path`** | Path to file |

## Functions

| **Returns** | **Name** | **Description** |
| :--- | :--- | :--- |
|  | [**`Close`**](file#close) | Closes the file |
|  | [**`Flush`**](file#flush) | Flushes content to the file |
| <BasicType.Boolean /> | [**`IsEOF`**](file#iseof) | Checks if the file status is End of File |
| <BasicType.Boolean /> | [**`IsBad`**](file#isbad) | Checks if the file status is Bad |
| <BasicType.Boolean /> | [**`IsGood`**](file#isgood) | Checks if the file status is Good |
| <BasicType.Boolean /> | [**`HasFailed`**](file#hasfailed) | Checks if the last operation has Failed |
| <BasicType.String /> | [**`Read`**](file#read) | Reads characters from the File and returns it. Also moves the file pointer to the latest read position. Pass 0 to read the whole file |
| <BasicType.String /> | [**`ReadLine`**](file#readline) | Reads and returns the next file line |
|  | [**`Seek`**](file#seek) | Sets the file pointer to a specific position |
| <BasicType.Number /> | [**`Size`**](file#size) | Returns the size of the file |
|  | [**`Skip`**](file#skip) | Skips n (amount) positions from the current file pointer position |
| <BasicType.Number /> | [**`Tell`**](file#tell) | Returns the current file pointer position |
|  | [**`Write`**](file#write) | Writes the Data at the current position of the file |

### `Close`

> Closes the file

```lua
my_file:Close()
```

### `Flush`

> Flushes content to the file

```lua
my_file:Flush()
```

### `IsEOF`

> Checks if the file status is End of File
>
> Returns <BasicType.Boolean />

```lua
my_file:IsEOF()
```

### `IsBad`

> Checks if the file status is Bad
>
> Returns <BasicType.Boolean />

```lua
my_file:IsBad()
```

### `IsGood`

> Checks if the file status is Good
>
> Returns <BasicType.Boolean />

```lua
my_file:IsGood()
```

### `HasFailed`

> Checks if the last operation has Failed
>
> Returns <BasicType.Boolean />

```lua
my_file:HasFailed()
```

### `Read`

> Reads n \(Length\) characters from the File and returns it. Also moves the file pointer to the latest read position. Pass 0 to read the whole file
>
> Returns <BasicType.String />

```lua
my_file:Read(length)
```

| Type | Parameter | Default Value | Description |
| :--- | :--- | :--- | :--- |
| <BasicType.Number /> | **`length`** | `0` | Length to be read from file |

### `ReadLine`

> Reads and returns the next file line
>
> Returns <BasicType.String />

```lua
my_file:ReadLine()
```

### `Seek`

> Sets the file pointer to a specific position

```lua
my_file:Seek(position)
```

| Type | Parameter | Description |
| :--- | :--- | :--- |
| <BasicType.Number /> | **`position`** | Position to  offset the file pointer |

### `Size`

> Returns the size of the file
>
> Returns <BasicType.Number />

```lua
my_file:Size()
```

### `Skip`

> Skips n \(amount\) positions from the current file pointer position

```lua
my_file:Skip(amount)
```

| Type | Parameter | Description |
| :--- | :--- | :--- |
| <BasicType.Number /> | **`amount`** | Amount to offset the file pointer |

### `Tell`

> Returns the current file pointer position
>
> Returns <BasicType.Number />

```lua
my_file:Tell()
```

### `Write`

> Writes the Data at the current position of the file

```lua
my_file:Write(data)
```

| Type | Parameter | Description |
| :--- | :--- | :--- |
| <BasicType.String /> | **`data`** | Writes the data to the file |

