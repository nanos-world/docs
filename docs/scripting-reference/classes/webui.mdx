---
title: 🖥️ WebUI
description: Class for spawning a web browser in the screen
sidebar_position: 0
tags: [scripting, ui]
---


Class for spawning a web browser in the screen.

import { Structs, Classes, Enums, BasicType, AuthorityType, ReferenceLink, AssetPath } from '@site/docs/components/_nanos.mdx';

![](/img/docs/webui.jpg)

:::info

🟧 **Authority:** This class can only be spawned on [Client](/core-concepts/scripting/authority-concepts.mdx).

:::

:::tip

This HTML implementation is built using the last versions of [Chromium Embedded Framework](https://bitbucket.org/chromiumembedded/cef).

:::


<ReferenceLink href="core-concepts/scripting/user-interface">User Interface</ReferenceLink>
<ReferenceLink href="getting-started/tutorials-and-examples/basic-hud-html">Basic HUD (HTML)</ReferenceLink>


## Usage

```lua title="Client/Index.lua"
-- Loading a local file
local my_ui = WebUI(
    "Awesome UI",            -- Name
    "file://UI/index.html",  -- Path relative to this package (Client/)
    true,                    -- Is Visible
)

-- Loading a Web URL
local my_browser = WebUI(
    "Awesome Site",        -- Name
    "https://nanos.world", -- Web's URL
    true,                  -- Is Visible
)

-- Loading a local file from other package
local my_ui = WebUI(
    "Awesome Other UI",      -- Name
    "file://other-package/Client/UI/index.html",
    true,                    -- Is Visible
)
```


#### Communicating between Lua and JS (WebUI)

```lua title="Client/Index.lua"
local my_ui = WebUI("Awesome UI", "file://UI/index.html")

local param1 = 123
local param2 = "hello"

-- Calls a JS event
my_ui:CallEvent("MyEvent", param1, param2)

-- Subscribes to receive JS events
my_ui:Subscribe("MyAnswer", function(param1)
    Package.Log("Received back! %s", param1)
    -- Will output 'Received back! Hey there!'
end)
```

```javascript
// Register for "MyEvent" from Lua
Events.Subscribe("MyEvent", function(param1, param2) {
    console.log("Triggered! " + param1 + " " + param2);
    // Will output 'Triggered! 123 hello'

    // Triggers "MyAnswer" on Lua
    Events.Call("MyAnswer", "Hey there!");
})
```


## Constructor Parameters

| **Type** | **Name** | **Default** | Description |
| :--- | :--- | :--- | :--- |
| <BasicType.String /> | **`name`** |  | Used for debugging logs |
| <BasicType.String /> | **`path`** |  | URL or `file://my_file.html` or <AssetPath.HTML /> |
| <BasicType.Boolean /> | **`is_visible`** | `true` | if WebUI is visible on screen |
| <BasicType.Boolean /> | **`is_transparent`** | `true` | if WebUI background is transparent |
| <BasicType.Boolean /> | **`auto_resize`** | `true` | if should auto resize when screen changes it's size (useful OFF when you are painting meshes with WebUI) |
| <BasicType.Number /> | **`width`** | `0` | size of the WebUI when you are not using auto_resize |
| <BasicType.Number /> | **`height`** | `0` | size of the WebUI when you are not using auto_resize |


:::tip

Loading a .html file supports the following searchers, which are looked in the following order:
1. Relative to `current-file-path/`
2. Relative to `current-package/Client/`
3. Relative to `current-package/`
4. Relative to `Packages/`

:::


## Functions

| **Returns** | **Name** | **Description** |
| :--- | :--- | :--- |
|  | [**`BringToFront`**](webui#bringtofront) | Puts this WebUI in the front of all WebUIs |
|  | [**`CallEvent`**](webui#callevent) | Calls an Event on the Browser’s JavaScript |
|  | [**`Destroy`**](webui#destroy) | Destroys this Browser |
|  | [**`LoadURL`**](webui#loadurl) | Loads a new File/URL in this Browser |
|  | [**`SetFreeze`**](webui#setfreeze) | Freezes the WebUI Rendering to the surface |
|  | [**`SetVisible`**](webui#setvisible) | Toggles the visibility |
| <Classes.Sound /> | [**`SpawnSound`**](webui#spawnsound) | Spawns a Sound entity to plays this WebUI sound |
|  | [**`SendMouseWheelEvent`**](webui#sendmousewheelevent) | Sends a Mouse Event into the WebUI programatically |
|  | [**`SendKeyEvent`**](webui#sendkeyevent) | Sends a Key Event into the WebUI programatically |
|  | [**`SendMouseMoveEvent`**](webui#sendmousemoveevent) | Sends a Mouse Move Event into the WebUI programatically |
|  | [**`SendMouseClickEvent`**](webui#sendmouseclickevent) | Sends a Mouse Click into the WebUI programatically |
| <BasicType.Boolean /> | [**`IsValid`**](webui#isvalid) | Returns if this entity is Valid |
| <BasicType.Number /> | [**`GetID`**](webui#getid) | Gets the network ID of this entity |
| <Structs.Vector2D /> | [**`GetSize`**](webui#getsize) | Gets the current WebUI size |
| <BasicType.String /> | [**`GetType`**](webui#gettype) | Returns the type of this Entity |
| <BasicType.Boolean /> | [**`IsVisible`**](webui#isvisible) | Returns if this WebUI is currently visible |
| <BasicType.Function /> | [**`Subscribe`**](webui#subscribe) | Subscribes for an Event |
|  | [**`Unsubscribe`**](webui#unsubscribe) | Unsubscribes from all subscribed Events in this Entity and in this Package |


### `BringToFront`

> Puts this WebUI in the front of all WebUIs

```lua
my_webui:BringToFront()
```


### `CallEvent`

> Calls an Event on the Browser's JavaScript

```lua
my_webui:CallEvent(event_name, arguments...)
```

| Type | Parameter | Description |
| :--- | :--- | :--- |
| <BasicType.String /> | **`event_name`** |  |
| <BasicType.Any /> | **`arguments...`** |  |


### `Destroy`

> Destroys this Browser

```lua
my_webui:Destroy()
```


### `LoadURL`

> Loads a new File/URL in this Browser
>
> Returns

```lua
my_webui:LoadURL(url)
```

| Type | Parameter | Description |
| :--- | :--- | :--- |
| <BasicType.String /> | **`url`** |  |


### `SetFreeze`

> Freezes the WebUI Rendering to the surface (it will still execute the JS under the hood)

```lua
my_webui:SetFreeze()
```


### `SetVisible`

> Toggles the visibility

```lua
my_webui:SetVisible(is_visible)
```

| Type | Parameter | Description |
| :--- | :--- | :--- |
| <BasicType.Boolean /> | **`is_visible`** |  |


### `SpawnSound`

> Spawns a Sound entity to plays this WebUI sound
>
> Returns a <Classes.Sound /> entity

```lua
my_webui:SpawnSound(location, is_2d, volume, inner_radius, falloff_distance, attenuation_function)
```

| Type | Parameter | Default Value | Description |
| :--- | :--- | :--- | :--- |
| <Structs.Vector /> | **`location`** | `Vector(0, 0, 0)` |  |
| <BasicType.Boolean /> | **`is_2d`** | `true` |  |
| <BasicType.Number /> | **`volume`** | `1.0` |  |
| <BasicType.Number /> | **`inner_radius`** | `400` |  |
| <BasicType.Number /> | **`falloff_distance`** | `3600` |  |
| <Enums>AttenuationFunction</Enums>  | **`attenuation_function`** | `Linear` |  |


### `SendMouseWheelEvent`

> Sends a Mouse Event into the WebUI programatically

| Type | Parameter | Default Value | Description |
| :--- | :--- | :--- | :--- |
| <BasicType.Number /> | **`mouse_x`** |  | Position X of the mouse |
| <BasicType.Number /> | **`mouse_y`** |  | Position Y of the mouse |
| <BasicType.Number /> | **`delta_x`** |  |  |
| <BasicType.Number /> | **`delta_y`** |  |  |


### `SendKeyEvent`

> Sends a Key Event into the WebUI programatically

| Type | Parameter | Default Value | Description |
| :--- | :--- | :--- | :--- |
| <Enums>WebUIKeyType</Enums> | **`key_type`** |  |  |
| <BasicType.Number /> | **`key_code`** |  |  |
| <Enums>WebUIModifier</Enums> | **`modifiers`** | `WebUIModifier.None` | Accept bitwise operators |


### `SendMouseMoveEvent`

> Sends a Mouse Move Event into the WebUI programatica

| Type | Parameter | Default Value | Description |
| :--- | :--- | :--- | :--- |
| <BasicType.Number /> | **`mouse_x`** |  | Position X of the mouse |
| <BasicType.Number /> | **`mouse_y`** |  | Position Y of the mouse |
| <Enums>WebUIModifier</Enums> | **`modifiers`** | `WebUIModifier.None` | Accept bitwise operators |
| <BasicType.Boolean /> | **`mouse_leave`** | `false` |  |


### `SendMouseClickEvent`

> Sends a Mouse Click into the WebUI programatically
> 
> You must send both Down and Up to make it work properly

| Type | Parameter | Default Value | Description |
| :--- | :--- | :--- | :--- |
| <BasicType.Number /> | **`mouse_x`** |  | Position X of the mouse |
| <BasicType.Number /> | **`mouse_y`** |  | Position Y of the mouse |
| <Enums>WebUIMouseType</Enums> | **`mouse_type`** |  | Which mouse button |
| <BasicType.Boolean /> | **`is_mouse_up`** |  | Whether the event was up or down |
| <Enums>WebUIModifier</Enums> | **`modifiers`** | `WebUIModifier.None` | Accept bitwise operators |
| <BasicType.Number /> | **`click_count`** | `1` | Use 2 for double click event |


### `IsValid`

> Gets if this entity is Valid
>
> Returns <BasicType.Boolean />

```lua
my_webui:IsValid()
```


### `GetID`

> Gets the network ID of this entity
>
> Returns <BasicType.Number />

```lua
my_webui:GetID()
```


### `GetSize`

> Gets the current size of this WebUI
>
> Returns <Structs.Vector2D />

```lua
my_webui:GetSize()
```


### `GetType`

> Returns the type of this Entity
>
> Returns <BasicType.String />

```lua
my_webui:GetType()
```


### `IsVisible`

> Returns if this WebUI is currently visible
>
> Returns <BasicType.Boolean />

```lua
my_webui:IsVisible()
```


### `Subscribe`

> Subscribes for an Event
> 
> Returns the <BasicType.Function /> callback itself

```lua
my_webui:Subscribe(event_name, callback)
```

| Type | Parameter | Default Value | Description |
| :--- | :--- | :--- | :--- |
| <BasicType.String /> | **`event_name`** |  |  |
| <BasicType.Function /> | **`function`** |  |  |


### `Unsubscribe`

> Unsubscribes from all subscribed Events in this Entity and in this Package, optionally passing the function to unsubscribe only that callback

```lua
my_webui:Unsubscribe(event_name, callback)
```

| Type | Parameter | Default Value | Description |
| :--- | :--- | :--- | :--- |
| <BasicType.String /> | **`event_name`** |  |  |
| <BasicType.Function /> | **`function`** | `nil` |  |

## Events

| **Name** | **Description** |
| :--- | :--- |
| [**`Failed`**](webui#failed) | When this page fails to load |
| [**`Ready`**](webui#ready) | When this page is fully loaded \(DOM and JavaScript\) |


### `Failed`

> Triggered when this page fails to load

```lua
my_webui:Subscribe("Failed", function(error_code, message)
    -- Load failed
end)
```

| Type | Parameter | Description |
| :--- | :--- | :--- |
| <BasicType.Number /> | **`error_code`** |  |
| <BasicType.String /> | **`message`** |  |


### `Ready`

> Triggered when this page is fully loaded

```lua
my_webui:Subscribe("Ready", function()
    -- When this page is fully loaded \(DOM and JavaScript\)
end)
```

