---
description: A Light represents a Lighting source
sidebar_position: 0
tags: [class]
---

# 💡 Light

A Light represents a Lighting source.

import { Structs, BasicType, AuthorityType, Classes, Enums } from '@site/docs/components/_nanos.mdx';

![](/img/docs/light.jpg)

nanos world provides 3 types of lights: **Spot**, **Point** and **Rect**. All lights are Dynamic and because of that, very expensive! Keep that in mind before spawning 1000 lights ;\).

:::tip

You can spawn this entity is both **Client** or **Server** \(if you spawn it on client, it won’t be synchronized with other players\).

:::

:::info

⏬ **Inheritance:** This class inherits from <Classes.Actor />. Please check it's pages for inherited **Methods** and **Events**.

:::

## Usage

```lua
local my_light = Light(
    Vector(-152, 245, 115),
    Rotator(0, 90, 90), -- Relevant only for Rect and Spot light types
    Color(1, 0, 0), -- Red Tint
    LightType.Point, -- Point Light type
    100, -- Intensity
    250, -- Attenuation Radius
    44, -- Cone Angle (Relevant only for Spot light type)
    0, -- Inner Cone Angle Percent (Relevant only for Spot light type)
    5000, -- Max Draw Distance (Good for performance - 0 for infinite)
    true, -- Whether to use physically based inverse squared distance falloff, where Attenuation Radius is only clamping the light's contribution. (Spot and Point types only)
    true, -- Cast Shadows?
    true -- Enabled?
)
```

## Constructor Parameters

| **Type** | **Name** | **Default** | Description |
| :--- | :--- | :--- | :--- |
| <Structs.Vector /> | **`location`** | `Vector(0, 0, 0)` |  |
| <Structs.Rotator /> | **`rotation`** | `Rotator(0, 0, 0)` | Relevant only for Rect and Spot LightTypes |
| <Structs.Color /> | **`color`** | `Color(1, 1, 1)` |  |
| <Enums>LightType</Enums> | **`light_type`** | `LightType.Point` |  |
| <BasicType.Number /> | **`intensity`** | `30` |  |
| <BasicType.Number /> | **`attenuation_radius`** | `250` |  |
| <BasicType.Number /> | **`cone_angle`** | `44` | Relevant only for Spot LightType |
| <BasicType.Number /> | **`inner_cone_angle_percent`** | `0` | Inner Cone Angle Percent (Relevant only for Spot LightType) (0-1) |
| <BasicType.Number /> | **`max_daw_distance`** | `5000` | Max Draw Distance (Good for performance - 0 for infinite |
| <BasicType.Boolean /> | **`use_inverse_squared_falloff`** | `true` | Whether to use physically based inverse squared distance falloff, where Attenuation Radius is only clamping the light's contribution. (Spot and Point types only) |
| <BasicType.Boolean /> | **`cast_shadows`** | `true` |  |
| <BasicType.Boolean /> | **`enabled`** | `true` |  |

## Functions

import BaseActorFunctions from '@site/docs/scripting-reference/classes/base-classes/_base-actor-functions.mdx';

<details><summary>Inherited Actor Functions</summary><BaseActorFunctions /></details>

| **Returns** | **Name** | **Description** |
| :--- | :--- | :--- |
|  | [**`SetColor`**](light#setcolor) | Sets the light color |

### `SetColor`

> Sets the light color

```lua
my_light:SetColor(color)
```

| Type | Parameter | Default Value | Description |
| :--- | :--- | :--- | :--- |
| <Structs.Color /> | **`color`** |  | The light color |

## Events

import BaseActorEvents from '@site/docs/scripting-reference/classes/base-classes/_base-actor-events.mdx';

<details><summary>Inherited Actor Events</summary><BaseActorEvents /></details>
