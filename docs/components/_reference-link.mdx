import { getActiveVersionPath } from '/docs/components/_active-version.mdx';

export const ReferenceLink = ({ children, href }) => (
	<a class="reference-link" href={`${getActiveVersionPath()}/${href}`}>
		{children}
		<span>
			{href}
		</span>
	</a>
);