import { getActiveVersionPath } from '/docs/components/_active-version.mdx';

<!-- Generic Base Authority component -->
export const BaseAuthority = (img, title) => (
	<a class="authority-availability" href={`${getActiveVersionPath()}/core-concepts/scripting/authority-concepts#methods-and-events-availability`}>
		<img src={img} title={title} />
	</a>
);

<!-- Defines Struct Types componenets -->
export const AuthorityOnly = () => BaseAuthority("/img/scripting/authority-only.svg", "authority-only - you can only call this method on the side which spawned it!");
export const ServerOnly = () => BaseAuthority("/img/scripting/server-only.svg", "server-only - you can only call this method on server side!");
export const ClientOnly = () => BaseAuthority("/img/scripting/client-only.svg", "client-only - you can only call this method on client side!");