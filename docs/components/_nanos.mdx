import { useActiveVersion } from '@docusaurus/plugin-content-docs/client';
import { Link } from "react-router-dom";

import Tippy from '@tippyjs/react';
import 'tippy.js/animations/scale-subtle.css';
import 'tippy.js/dist/tippy.css';


export const ClassesData = {
};


<!-- Getter to get the current version path - for links -->
export const getActiveVersionPath = () => {
	const activeVersion = useActiveVersion();
	return activeVersion.path;
};


<!-- Square Card Link -->
export const CardLink = ({ title, description, href, image_src }) => (
	<a class="card-link" href={href.startsWith("http") ? href : `${getActiveVersionPath()}/${href}`} > 
		<img src={image_src ? image_src : '/img/docs/nanos-world-background.jpg'} />
		<h4>{title}</h4>
		<p>{description}</p>
	</a>
);

<!-- Rectangular Reference Link -->
export const ReferenceLink = ({ children, href }) => (
	<a class="reference-link" href={href.startsWith("http") ? href : `${getActiveVersionPath()}/${href}`}>
		{children}
		<span>
			{href}
		</span>
	</a>
);


export const AuthorityTooltip = ({ img, title, description }) => (
	<>
		<h3>
			<img src={img} title={title} style={{ width: "24px", "vertical-align": "middle", "margin-right": "5px" }} /> {title}
		</h3>
		{description}
	</>
);

export const AssetPathToolTip = ({ label, description }) => (
	<>
		<h3>
			{label}
		</h3>
		<span dangerouslySetInnerHTML={{ __html: description }}></span>
	</>
);

export const ClassToolTip = ({ class_name, description }) => (
	<>
		<h3>
			{class_name} Class
		</h3>
		<span dangerouslySetInnerHTML={{ __html: description }}></span>
	</>
);

export const StructToolTip = ({ struct_name, description }) => (
	<>
		<h3>
			{struct_name} Struct
		</h3>
		<span dangerouslySetInnerHTML={{ __html: description }}></span>
	</>
);

export const EnumToolTip = ({ enum_name }) => (
	<EnumDeclaration enum_name={ enum_name } is_tooltip={true} />
);

<!-- Generic Base Authority component -->
export const BaseAuthority = (img, title, description) => (
	<Tippy interactive={true} maxWidth={400} animation={"scale-subtle"} placement={"left"} content={<AuthorityTooltip img={img} title={title} description={description} />}>
		<Link class="authority-availability" to={`${getActiveVersionPath()}/core-concepts/scripting/authority-concepts#methods-and-events-availability`}>
			<img src={img} title={title} />
		</Link>
	</Tippy>
);

<!-- Generic Basic Type component -->
export const BaseType = (label) => (
	<a href={`${getActiveVersionPath()}/scripting-reference/glossary/basic-types#${label.toLowerCase()}`}>{label}</a>
);

<!-- Generic Struct Type component -->
export const BaseStruct = (label, path, description) => (
	<Tippy interactive={true} maxWidth={300} animation={"scale-subtle"} placement={"left"} content={<StructToolTip struct_name={label} description={description} />}>
		<Link to={`${getActiveVersionPath()}/scripting-reference/utility-classes/${path ? path : label.toLowerCase()}`}>{label}</Link>
	</Tippy>
);

<!-- Generic Struct Type component -->
export const BaseClass = (label, emoji, path) => (
	<Tippy interactive={true} maxWidth={400} animation={"scale-subtle"} placement={"left"}
		content={<ClassToolTip class_name={`${emoji} ${label}`} description={ ClassesData[label] ? ClassesData[label].description : "Class not found. Soon™." } />}>
		<Link to={`${getActiveVersionPath()}/scripting-reference/classes/${path ? path : label.toLowerCase()}`}>{label}</Link>
	</Tippy>
);

<!-- Define Enum component -->
export const Enums = ({ children }) => (
	<Tippy interactive={true} maxWidth={600} animation={"scale-subtle"} placement={"left"} content={<EnumToolTip enum_name={ children } />}>
		<Link to={`${getActiveVersionPath()}/scripting-reference/glossary/enums#${children.toLowerCase()}`} >{children}</Link>
	</Tippy>
);

<!-- Define Asset Path component -->
export const BaseAssetPath = (type, label, description) => (
	<Tippy interactive={true} maxWidth={400} animation={"scale-subtle"} placement={"left"} content={<AssetPathToolTip label={label} description={description} />}>
		<Link to={`${getActiveVersionPath()}/core-concepts/assets#referencing-assets-in-scripting`}>{label}</Link>
	</Tippy>
);

<!-- Define Special Path component -->
export const SpecialPath = (type, label, description) => (
	<Tippy interactive={true} maxWidth={400} animation={"scale-subtle"} placement={"left"} content={<AssetPathToolTip label={label} description={description} />}>
		<Link to={`${getActiveVersionPath()}/scripting-reference/glossary/basic-types#specialpath`}>{label}</Link>
	</Tippy>
);


<!-- Defines Authority Types components -->
export const AuthorityType = {
	AuthorityOnly: () => BaseAuthority("/img/scripting/authority-only.svg", "Authority Only", "This can be called only on the side which spawned it!"),
	ServerOnly: () => BaseAuthority("/img/scripting/server-only.svg", "Server Only", "This can be called only on server side!"),
	ClientOnly: () => BaseAuthority("/img/scripting/client-only.svg", "Client Only", "This can be called only on client side!"),
	Both: () => BaseAuthority("/img/scripting/both.png", "Both Sides", "This can be called on any side!"),
};

<!-- Defines Basic Types components -->
export const BasicType = {
	Number: () => BaseType("number"),
	String: () => BaseType("string"),
	Boolean: () => BaseType("boolean"),
	Table: () => BaseType("table"),
	Iterator: () => BaseType("iterator"),
	Any: () => BaseType("any"),
	Function: () => BaseType("function"),
	Nil: () => BaseType("nil"),
};

<!-- Defines Struct Types components -->
export const Structs = {
	Vector: () => BaseStruct("Vector", "", "A vector composed of components (X, Y, Z) with floating point precision."),
	Rotator: () => BaseStruct("Rotator", "", "A container for rotation information (Pitch, Yaw, Roll). All rotation values are stored in degrees."),
	Color: () => BaseStruct("Color", "", "A color composed of components (R, G, B, A) with floating point precision."),
	Quat: () => BaseStruct("Quat", "quaternion", "Floating point Quaternion (X, Y, Z, W) that can represent a rotation about an axis in 3-D space."),
	Vector2D: () => BaseStruct("Vector2D", "vectortwod", "A Vector2D composed of components (X, Y) with floating point precision. Used mainly for HUD and Drawing on screen."),
};

<!-- Defines Classes components -->
export const Classes = {
	Actor: () => BaseClass("Base Actor", "🎭", "base-classes/actor"),
	Paintable: () => BaseClass("Base Paintable", "🎨", "base-classes/paintable"),
	Pickable: () => BaseClass("Base Pickable", "⚔️", "base-classes/pickable"),
	Billboard: () => BaseClass("Billboard", "🪧"),
	Blueprint: () => BaseClass("Blueprint", "🔣"),
	Canvas: () => BaseClass("Canvas", "🖼️"),
	Cable: () => BaseClass("Cable", "⛓️"),
	Character: () => BaseClass("Character", "🧑"),
	Database: () => BaseClass("Database", "💾"),
	Decal: () => BaseClass("Decal", "🖌️"),
	File: () => BaseClass("File", "📁"),
	Grenade: () => BaseClass("Grenade", "💣"),
	Light: () => BaseClass("Light", "💡"),
	Melee: () => BaseClass("Melee", "🔪"),
	Particle: () => BaseClass("Particle", "✨"),
	Player: () => BaseClass("Player", "👩‍💻"),
	Prop: () => BaseClass("Prop", "📦"),
	SceneCapture: () => BaseClass("SceneCapture", "📽️", "scene-capture"),
	Sound: () => BaseClass("Sound", "🔊"),
	StaticMesh: () => BaseClass("StaticMesh", "🏠", "static-mesh"),
	TextRender: () => BaseClass("TextRender", "🆒", "text-render"),
	Trigger: () => BaseClass("Trigger", "🪤"),
	Vehicle: () => BaseClass("Vehicle", "🚙"),
	Weapon: () => BaseClass("Weapon", "🔫"),
	WebUI: () => BaseClass("WebUI", "🖥️"),
};

<!-- Defines Assets Path -->
export const AssetPath = {
	StaticMesh: () => BaseAssetPath("StaticMesh", "StaticMesh Reference", "StaticMesh Asset Reference in the format: <code>asset-pack::SM_MyAsset</code>."),
	SkeletalMesh: () => BaseAssetPath("SkeletalMesh", "SkeletalMesh Reference", "SkeletalMesh Asset Reference in the format: <code>asset-pack::SM_MyAsset</code>."),
	Animation: () => BaseAssetPath("Animation", "Animation Reference", "Animation Asset Reference in the format: <code>asset-pack::SM_MyAsset</code>."),
	Particle: () => BaseAssetPath("Particle", "Particle Reference", "Particle Asset Reference in the format: <code>asset-pack::SM_MyAsset</code>."),
	Material: () => BaseAssetPath("Material", "Material Reference", "Material Asset Reference in the format: <code>asset-pack::SM_MyAsset</code>."),
	Map: () => BaseAssetPath("Map", "Map Reference", "Map Asset Reference in the format: <code>asset-pack::SM_MyAsset</code>."),
	Blueprint: () => BaseAssetPath("Blueprint", "Blueprint Reference", "Blueprint Asset Reference in the format: <code>asset-pack::SM_MyAsset</code>."),
	Sound: () => SpecialPath("Sound", "Sound Reference", "Sound Asset Reference in the format:<br/><code>asset-pack::SM_MyAsset</code><br/><br/>Or as Special Path:<br/><code>package://my-package/Client/sound.ogg</code>."),
	Image: () => SpecialPath("Image", "Image Path", "Image Path as Special Path:<br/><code>package://my-package/Client/sound.ogg</code>."),
	HTML: () => SpecialPath("HTML", "HTML Path", "HTML Path as Special Path:<br/><code>package://my-package/Client/file.html</code>."),
};