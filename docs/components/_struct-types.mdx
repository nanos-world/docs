import { getActiveVersionPath } from '/docs/components/_active-version.mdx';

<!-- Generic Struct Type component -->
export const StructType = (label, path) => (
	<a href={`${getActiveVersionPath()}/scripting-reference/utility-classes/${path ? path : label.toLowerCase()}`}>{label}</a>
);

<!-- Defines Struct Types componenets -->
export const Vector = () => StructType("Vector");
export const Rotator = () => StructType("Rotator");
export const Color = () => StructType("Color");
export const Quat = () => StructType("Quat", "quaternion");
export const Vector2D = () => StructType("Vector2D", "vectortwod");